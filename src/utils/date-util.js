// import * as moment from "vue-moment";

export default {
  DAY: ["일", "월", "화", "수", "목", "금", "토"],

  /**
   * description
   * @param {*} param1		param description
   * @return return value
   */
  getDateFormatDash(val) {
    return;
  },
  // getYear(date) {
  //   return moment(date).format("YYYY");
  // },
};
